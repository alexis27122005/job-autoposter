{
  "services": [
    {
      "name": "backend",
      "type": "web",
      "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd backend && pip install -r requirements.txt && python manage.py migrate"
      },
      "startCommand": "cd backend && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT"
    },
    {
      "name": "celery-worker",
      "type": "worker",
      "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd backend && pip install -r requirements.txt"
      },
      "startCommand": "cd backend && celery -A backend worker -l info"
    }
  ]
}